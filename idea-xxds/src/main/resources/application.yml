spring:
  datasource:
    url: jdbc:mysql://localhost:3306/linear_algebra?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    deepseek:
      api-key: sk-444d4a817e4e48d8adfdce05baa0664c   # 替换为你的真实 API Key
      chat:
        options:
          model: deepseek-chat          # 或 deepseek-reasoner
          temperature: 0.7

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl